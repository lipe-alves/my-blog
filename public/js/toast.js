(() => {
    /**
     * @typedef ToastConfig
     * @type {{
     *      duration: number;
     * }}
     */

    window.myBlog.toast = {
        show() {

        },
        /**
         * @param {string} message 
         */
        success(message, config = {}) {


        }
    };
})();